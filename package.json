{
  "private": true,
  "workspaces": [
    "recipes/*"
  ],
  "scripts": {
    "css": "yarn --cwd recipes/css build",
    "global-constant": "yarn --cwd recipes/global-constant build"
  },
  "engines": {
    "node": ">=8",
    "yarn": "1"
  },
  "license": "MIT"
}
